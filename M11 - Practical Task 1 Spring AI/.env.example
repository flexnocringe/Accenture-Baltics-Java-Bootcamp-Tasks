# Copy this file to .env to override defaults.
# The application reads these automatically at startup via spring-dotenv.

# Optional overrides (defaults are fine for local dev with Ollama)
#OLLAMA_BASE_URL=http://localhost:11434
#OLLAMA_MODEL=gemma3:1b
#AI_SYSTEM_PROMPT=You are a helpful, concise assistant.
#SQLITE_DB_PATH=chatbot.db
